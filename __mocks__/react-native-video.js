// The native video surface can't mount under Jest; the detail sheet only needs
// the component to exist so it can decide between player, play button and the
// "no clip" message.
module.exports = { __esModule: true, default: () => null };
